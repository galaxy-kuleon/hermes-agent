# Round 14 Visual/Layout Penalty + Text Coverage Gate — 2026-06-10

Background process `proc_34964fb75579` completed successfully:

```text
out: /tmp/pdfdocx5-round14-table-priority-next/auto-battle-report.json
```

It selected three HK registered-design e-form PDFs. After Round13 form-field grouping, their semantic table counts all looked perfect:

```json
{
  "page_count_delta": 0,
  "docx_content_tables": 12,
  "source_tables_detected": 12,
  "content_table_count_delta": 0,
  "failure_score": {"score": 0, "severity": "ok"}
}
```

## Problem Found

Visual inspection of the contact sheet for `[HKIPRD0003] 20140725 eform_D2_Design 1_1401388.8.pdf` showed this was a false OK:

```text
/private/tmp/pdfdocx5-round14-table-priority-next/[HKIPRD0003]_20140725_eform_D2_Design_1_1401388.8/refine/qa_turn_1/contact_sheet.png
```

Human QA finding:

- source page 1 is densely filled across most of the page
- rendered DOCX page 1 only occupies the top ~1/3 of the page
- large lower content region is missing/reflowed away from page 1
- table count parity is correct, but visual/layout fidelity is unacceptable

Cheap visual RMSE did not catch this:

```json
{
  "rmse_rgb_delta": 37.56,
  "changed_channel_fraction_gt24": 0.20661
}
```

Because whitespace dominates RMSE.

## Fix Implemented

### 1. Added visual ink/layout metrics

New functions in `scripts/pdfdocx5.py`:

```python
def image_ink_metrics(img, threshold=245) -> dict

def visual_layout_metrics(a_path, b_path, size=(512,512)) -> dict
```

`visual_diff_metrics` entries now include:

```json
{
  "page": 1,
  "source_vs_rendered": {
    "rmse_rgb_delta": 37.56,
    "changed_channel_fraction_gt24": 0.20661
  },
  "layout": {
    "source_ink": {
      "ink_fraction": 0.20259,
      "bbox_norm": [0.043, 0.0234, 0.9668, 0.9316],
      "height_fraction": 0.91016
    },
    "rendered_ink": {
      "ink_fraction": 0.06925,
      "bbox_norm": [0.0898, 0.082, 0.9082, 0.3652],
      "height_fraction": 0.28516
    },
    "max_abs_ink_bbox_delta": 0.5664,
    "rendered_to_source_ink_ratio": 0.3418,
    "rendered_to_source_height_ratio": 0.3133
  }
}
```

### 2. Added failure score penalties

New penalty reasons:

```text
visual_changed_fraction_high / medium
visual_ink_bbox_shift
visual_ink_height_ratio_bad
```

### 3. Fixed source text coverage bug

`qa_failure_score()` was reading `pdf_probe.text_chars`, but the actual dataclass field is `total_text_chars`. Fixed:

```python
source_text = int(source_probe.get("total_text_chars", source_probe.get("text_chars", 0)) or 0)
```

Added editable text coverage penalty:

```text
source_text > 1000 and visible/source < 0.65 -> editable_text_coverage_low, +25
source_text > 1000 and visible/source < 0.80 -> editable_text_coverage_medium, +10
```

## Verification: HKIPRD0003 False OK Now Scores Major

Rechecked existing refined DOCX:

```bash
qa_source_subset.pdf + refine/turn_1.docx -> qa_recheck_text_visual_penalty2
```

Result:

```json
{
  "failure_score": {
    "score": 65,
    "severity": "major",
    "reasons": [
      {
        "type": "editable_text_coverage_low",
        "visible_text_chars": 7062,
        "source_text_chars": 14234,
        "coverage_ratio": 0.4961,
        "points": 25
      },
      {
        "type": "visual_changed_fraction_high",
        "page": 1,
        "changed_channel_fraction_gt24": 0.20661,
        "points": 12
      },
      {
        "type": "visual_ink_bbox_shift",
        "page": 1,
        "max_abs_ink_bbox_delta": 0.5664,
        "points": 16
      },
      {
        "type": "visual_ink_height_ratio_bad",
        "page": 1,
        "rendered_to_source_height_ratio": 0.3133,
        "points": 12
      }
    ]
  }
}
```

This is the desired behavior: semantic table parity can be OK while the document is still rejected for missing/reflowed visual content.

## Regression Smoke

Strict smoke still passed:

```bash
~/.hermes/skills/productivity/pdf-docx-five-turn-refiner/scripts/smoke_pdfdocx5.py \
  --work-dir /tmp/pdfdocx5-round14-smoke2 \
  --history-dir /tmp/pdfdocx5-round14-history2 \
  --strict-table-geometry
```

Result:

```json
{
  "ok": true,
  "result": "/tmp/pdfdocx5-round14-smoke2/smoke-result.json"
}
```

## Current Diagnosis

The HK registered-design forms are not table-count failures anymore. They are now correctly identified as:

```text
editable text coverage + visual ink/layout coverage failures
```

The likely root cause is `pdf2docx` skipping overlapped lines during analysis:

```text
[WARNING] Ignore Line "Application for registration of designs" due to overlap
[WARNING] Ignore Line "Use of Personal data" due to overlap
[WARNING] Ignore Line "Any of the information provided may be made available for public inspection" due to overlap
...
```

So next repair target is not table classification; it is content recovery for overlapped form text, likely via one of:

1. pdf2docx parameter tuning if available
2. fallback text overlay/append for missing source text
3. mixed XA+XB page-level fallback for pages with low editable coverage
4. q7/GLM OCR-assisted form text repair

## Artifacts

```text
/tmp/pdfdocx5-round14-table-priority-next/auto-battle-report.json
/tmp/pdfdocx5-round14-table-priority-next/[HKIPRD0003]_20140725_eform_D2_Design_1_1401388.8/qa_recheck_text_visual_penalty2/
/tmp/pdfdocx5-round14-smoke2/smoke-result.json
```
